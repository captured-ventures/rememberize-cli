# Go build output
/rememberize
/rememberize.exe
/dist/
/bin/

# Go coverage / profiling
*.out
*.test
*.prof
coverage.html

# User-facing config (tests should never leak into repo)
.rememberize/

# IDE / editor
.idea/
.vscode/
*.swp

# Claude Code — local overrides only (committed settings.json is shared)
.claude/settings.local.json
.claude/scheduled_tasks.lock

# OS
.DS_Store
Thumbs.db
